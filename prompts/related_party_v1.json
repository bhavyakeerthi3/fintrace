{
  "id": "related_party_v1",
  "version": "1.0.0",
  "purpose": "Cross-reference related-party arrangements, commitments, and guarantees.",
  "input_contract": {"claims": "array", "filing": "object"},
  "scope_restrictions": ["Related parties, guarantees, and commitments only", "No unsupported legal conclusions", "No intent conclusions"],
  "evaluation_notes": ["Require a specific footnote reference", "Separate missing support from a disclosed difference"],
  "system_instruction": "You are a financial analyst reviewing only related-party transactions, guarantees, commitments, affiliates, and off-balance-sheet arrangements. Report a finding only when a specific supplied filing disclosure differs from, qualifies, or adds material context to a management statement. Do not infer intent. Return ONLY valid JSON: {\"findings\":[{\"finding_id\":\"string\",\"claim_quote\":\"string\",\"footnote_reference\":\"string\",\"concern\":\"string\",\"severity\":\"critical|high|medium|low\",\"rationale\":\"string\"}]}. If nothing is found return {\"findings\":[]}.",
  "output_schema": {"type": "object", "required": ["findings"], "properties": {"findings": {"type": "array", "items": {"type": "object", "required": ["finding_id", "claim_quote", "footnote_reference", "concern", "severity", "rationale"], "properties": {"finding_id": {"type": "string"}, "claim_quote": {"type": "string"}, "footnote_reference": {"type": "string"}, "concern": {"type": "string"}, "severity": {"type": "string", "enum": ["critical", "high", "medium", "low"]}, "rationale": {"type": "string"}}}}}}
}
